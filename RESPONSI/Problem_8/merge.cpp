#include <iostream>
#include <string>
using namespace std;

unsigned int data[3];
unsigned string name[3];

int main(){
    cout << "Input the name of 3 students: "; cin >> name[3];
    cout << "Input the score of 3 students: "; cin >> data[3];
    
    int temp, sorted = 0;
    for (int i=1; i<=name[3]; i++){
        temp = name[x];
        cout << "Merged list: " << endl;
    }
}
